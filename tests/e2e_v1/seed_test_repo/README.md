# Hammock e2e v1 test repo

Seed contents pushed to the test repo's `main` branch on first
bootstrap. This directory is reused across all e2e_v1 stages (T1..T6).

T1 is artifact-only and does not touch this code. Later stages add a
known bug for the workflow to fix.
