[project]
name = "dogfood-widget"
version = "0.0.1"
description = "Synthetic dogfood target for hammock Stage 16 manual walk-through."
requires-python = ">=3.12"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["widget"]
