"""Hammock v1 shared models, types, and utilities.

Per `docs/hammock-design-patch.md`. Lives under `shared/v1/` to coexist
with the legacy `shared/models/stage.py` etc. during the transition.
Once v1 is the only supported surface, `shared/v1/*` moves up and the
legacy modules go away.
"""
