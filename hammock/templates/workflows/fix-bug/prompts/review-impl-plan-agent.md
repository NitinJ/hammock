Review the supplied implementation plan.

Approve when the plan's steps are individually shippable, ordered correctly, and collectively cover everything the impl spec requires. Request changes when steps are too coarse to land as separate PRs, dependencies between steps are wrong, or the plan omits work the spec calls out.

Return your verdict with a short, specific reason.
