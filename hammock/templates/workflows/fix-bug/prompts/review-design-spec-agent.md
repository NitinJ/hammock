Review the supplied design spec.

Decide whether to **approve** or **request changes**:
- Approve when the spec is concrete, grounded in entities that actually exist in the codebase, and would let an implementer proceed without further clarification.
- Request changes when the spec is vague, references non-existent symbols, omits an important risk, or proposes an approach that would not address the bug.

Return your verdict with a short, specific reason. Approval is the default only when the spec genuinely meets the bar — do not approve out of politeness.
