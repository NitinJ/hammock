"""Hammock bundled assets package.

Contains the Stop-hook script, job templates, and UI form templates
that the runtime resolves via :mod:`importlib.resources`. Listed in
``[tool.hatch.build.targets.wheel].packages`` so installed runs find
the bundled files alongside the Python code.
"""
